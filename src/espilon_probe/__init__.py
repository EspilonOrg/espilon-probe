"""probe - one CLI for the physical layer (RF + hardware buses).

Same commands in an Espilon lab (virtual backend) and on real hardware (hci, killerbee,
sdr, openocd, ...). See ARCHITECTURE.md.
"""

__version__ = "0.0.0"

"""probe - one CLI for the physical layer (RF + hardware buses).

Same commands against a virtual target (virtual backend) and on real hardware (hci,
killerbee, sdr, openocd, ...). See ARCHITECTURE.md.
"""

__version__ = "0.1.0"

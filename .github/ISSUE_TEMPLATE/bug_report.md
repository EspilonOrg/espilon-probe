---
name: Bug report
about: Report a defect in probe
title: "bug: "
labels: bug
---

## What happened

A clear description of the bug.

## Steps to reproduce

The exact `probe` command line(s), and the backend/target in use (`virtual`, `socketcan`,
`serial`).

```
probe ...
```

## Expected vs actual

- Expected:
- Actual (paste the full output; if it is a traceback, include all of it):

## Environment

- probe version (`pip show espilon-probe`):
- Python version (`python --version`):
- OS:

## Additional context

Anything else that helps (a minimal capture, the `probe info` output, etc). Do not attach
sensitive data.

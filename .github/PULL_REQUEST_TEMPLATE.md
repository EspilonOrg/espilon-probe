## What

Briefly, what this change does.

## Why

The motivation or the issue it fixes (link it).

## Checklist

- [ ] Tests pass locally (`python -m pytest tests/ -q`).
- [ ] Behavior changes are covered by new or updated tests.
- [ ] The client core stays stdlib only (no new third-party dependency in
      `src/espilon_probe/`; real-hardware deps go behind an optional extra).
- [ ] No target-specific content added to the client.
- [ ] Conventional Commit title; docs/CHANGELOG updated where relevant.
